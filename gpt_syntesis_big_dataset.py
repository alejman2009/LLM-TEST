# -*- coding: utf-8 -*-
"""gpt-Syntesis_big_dataset.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1ypl6UFU9hhZHBg8fiRGQoaq_v4QhtLjR

# Dataset

## Installing Required Software

Support for third party widgets will remain active for the duration of the session. To disable support:
"""

#!pip install datasets
#!pip install google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client

"""## Preparing the dataset"""

import os
from tqdm import tqdm

'''
from google.colab import userdata
from google.colab import drive
userdata.get('LLM')
drive.mount('/content/drive')

os.chdir("/content/drive/MyDrive/compartir/Documentacion/TFG/intento_collab/Datasets")
'''

os.chdir("G:/UNIVERSIDAD/4ºCurso/TFG/ENTORNO-ML/Datasets")

"""Support for third party widgets will remain active for the duration of the session. To disable support:"""

from datasets import load_dataset
dataset = load_dataset("Skylion007/openwebtext", split="train",cache_dir="G:/UNIVERSIDAD/4ºCurso/TFG/ENTORNO-ML/Datasets/cache", trust_remote_code=True)
print("Done...\n")

"""#Procesado del dataset"""

text = ""
for data in tqdm(dataset):
    text += data['text']

chars = sorted(list(set(text)))
vocab_size = len(chars)
print(chars)

"""Almacenamiento del dataset como un formato .pkl"""

import pickle
dataset_pkl_file = "LLM_Dataset_opewebtext_full.pkl"
with open(dataset_pkl_file, 'wb') as file:
    pickle.dump(text, file)